// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: legal (1.0.0)
//   manifest: cli-manifest/legal.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using System.Text.Json.Nodes;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.Legal.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>Legal service — LegalTags, which govern how data may be used.</summary>
public static partial class LegalCommands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagList());
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagGet());
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagAdd());
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagUpdate());
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagDelete());
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagValidate());
        tree.Node("legaltag").Subcommands.Add(BuildLegaltagProperties());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>List LegalTags in the partition.</summary>
    /// <remarks>GET /legaltags on the Legal service.</remarks>
    private static Command BuildLegaltagList()
    {
        var validOption = new Option<bool?>("--valid")
        {
            Description = "Return only valid tags (true) or only invalid ones (false). Omit for all.",
        };

        var command = new Command("list", "List LegalTags in the partition.");
        command.Options.Add(validOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var valid = parseResult.GetValue(validOption);

            var result = await context.Client.Legal.Legaltags.GetAsync(configuration =>
            {
                configuration.QueryParameters.Valid = valid;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("legalTags", ("Name", "name"), ("Description", "description")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Get a LegalTag by name.</summary>
    /// <remarks>GET /legaltags/{name} on the Legal service.</remarks>
    private static Command BuildLegaltagGet()
    {
        var nameOption = new Option<string>("--name", "-n")
        {
            Description = "LegalTag name.",
            Required = true,
        };

        var command = new Command("get", "Get a LegalTag by name.");
        command.Options.Add(nameOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var name = parseResult.GetValue(nameOption)!;

            var result = await context.Client.Legal.Legaltags[name].GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Create a LegalTag from a JSON file.</summary>
    /// <remarks>POST /legaltags on the Legal service.</remarks>
    private static Command BuildLegaltagAdd()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the LegalTag definition.",
            Required = true,
        };

        var command = new Command("add", "Create a LegalTag from a JSON file.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<LegalTagDto>(
                bodyJson, LegalTagDto.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Legal.Legaltags.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Name", "name"), ("Description", "description")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Update an existing LegalTag from a JSON file.</summary>
    /// <remarks>PUT /legaltags on the Legal service.</remarks>
    private static Command BuildLegaltagUpdate()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the LegalTag changes.",
            Required = true,
        };

        var command = new Command("update", "Update an existing LegalTag from a JSON file.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<UpdateLegalTag>(
                bodyJson, UpdateLegalTag.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Legal.Legaltags.PutAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Name", "name"), ("Description", "description")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Delete a LegalTag by name.</summary>
    /// <remarks>DELETE /legaltags/{name} on the Legal service.</remarks>
    private static Command BuildLegaltagDelete()
    {
        var nameOption = new Option<string>("--name", "-n")
        {
            Description = "LegalTag name to delete.",
            Required = true,
        };

        var command = new Command("delete", "Delete a LegalTag by name.");
        command.Options.Add(nameOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var name = parseResult.GetValue(nameOption)!;

            await context.Client.Legal.Legaltags[name].DeleteAsync(cancellationToken: cancellationToken);

            return context.Output.WriteMessage("LegalTag deleted");
        }, cancellationToken));

        return command;
    }

    /// <summary>Check which of the given LegalTag names are invalid, and why.</summary>
    /// <remarks>POST /legaltags:validate on the Legal service.</remarks>
    private static Command BuildLegaltagValidate()
    {
        var namesBodyOption = new Option<string[]>("--name", "-n")
        {
            Description = "LegalTag name to check. Repeat for several.",
            Required = true,
            AllowMultipleArgumentsPerToken = true,
        };

        var command = new Command("validate", "Check which of the given LegalTag names are invalid, and why.");
        command.Options.Add(namesBodyOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyNode = new JsonObject();
            var namesValue = parseResult.GetValue(namesBodyOption)!;
            bodyNode["names"] = new JsonArray(namesValue.Select(item => (JsonNode)JsonValue.Create(item)!).ToArray());
            var bodyJson = bodyNode.ToJsonString();
            var body = await KiotaJsonSerializer.DeserializeAsync<RequestLegalTags>(
                bodyJson, RequestLegalTags.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Legal.LegaltagsValidate.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("invalidLegalTags", ("Name", "name"), ("Reason", "reason")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Show the allowed values for LegalTag properties.</summary>
    /// <remarks>GET /legaltags:properties on the Legal service.</remarks>
    private static Command BuildLegaltagProperties()
    {

        var command = new Command("properties", "Show the allowed values for LegalTag properties.");

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {

            var result = await context.Client.Legal.LegaltagsProperties.GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }
}
