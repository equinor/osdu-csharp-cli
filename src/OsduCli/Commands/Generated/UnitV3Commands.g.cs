// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: unit/v3 (3.0.0)
//   manifest: cli-manifest/unit_v3.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using System.Text.Json.Nodes;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.UnitV3.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>Unit service — unit catalog, measurements and conversions.</summary>
public static partial class UnitV3Commands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("unit").Subcommands.Add(BuildUnitList());
        tree.Node("unit").Subcommands.Add(BuildUnitGet());
        tree.Node("unit").Subcommands.Add(BuildUnitSearch());
        tree.Node("unit").Subcommands.Add(BuildUnitBySymbol());
        tree.Node("unit").Subcommands.Add(BuildUnitByMeasurement());
        tree.Node("unit").Subcommands.Add(BuildUnitBySystem());
        tree.Node("unit").Subcommands.Add(BuildUnitPreferred());
        tree.Node("unit").Subcommands.Add(BuildUnitMaps());
        tree.Node("unit conversion").Subcommands.Add(BuildUnitConversionScale());
        tree.Node("unit conversion").Subcommands.Add(BuildUnitConversionAbcd());
        tree.Node("unit catalog").Subcommands.Add(BuildUnitCatalogGet());
        tree.Node("unit catalog").Subcommands.Add(BuildUnitCatalogSearch());
        tree.Node("unit catalog").Subcommands.Add(BuildUnitCatalogMapStates());
        tree.Node("unit catalog").Subcommands.Add(BuildUnitCatalogLastModified());
        tree.Node("measurement").Subcommands.Add(BuildMeasurementList());
        tree.Node("measurement").Subcommands.Add(BuildMeasurementGet());
        tree.Node("measurement").Subcommands.Add(BuildMeasurementSearch());
        tree.Node("measurement").Subcommands.Add(BuildMeasurementMaps());
        tree.Node("unit-system").Subcommands.Add(BuildUnitSystemList());
        tree.Node("unit-system").Subcommands.Add(BuildUnitSystemGet());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>List all units in the catalog.</summary>
    /// <remarks>GET /v3/unit on the UnitV3 service.</remarks>
    private static Command BuildUnitList()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of units to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of units to return.",
        };

        var command = new Command("list", "List all units in the catalog.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Unit.GetAsync(configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("units", ("Name", "name"), ("Symbol", "displaySymbol"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Get one unit by namespace and symbol.</summary>
    /// <remarks>GET /v3/unit/symbol on the UnitV3 service.</remarks>
    private static Command BuildUnitGet()
    {
        var namespacesOption = new Option<string>("--namespace")
        {
            Description = "Namespace to look in, e.g. Energistics_UoM.",
            Required = true,
        };
        var symbolOption = new Option<string>("--symbol", "-s")
        {
            Description = "Unit symbol, e.g. m or ft.",
            Required = true,
        };

        var command = new Command("get", "Get one unit by namespace and symbol.");
        command.Options.Add(namespacesOption);
        command.Options.Add(symbolOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var namespaces = parseResult.GetValue(namespacesOption)!;
            var symbol = parseResult.GetValue(symbolOption)!;

            var result = await context.Client.UnitV3.V3.Unit.Symbol.GetAsync(configuration =>
            {
                configuration.QueryParameters.Namespaces = namespaces;
                configuration.QueryParameters.Symbol = symbol;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Search the unit catalog.</summary>
    /// <remarks>POST /v3/unit/search on the UnitV3 service.</remarks>
    private static Command BuildUnitSearch()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of results to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of results to return.",
        };
        var queryBodyOption = new Option<string>("--query", "-q")
        {
            Description = "Search expression.",
            Required = true,
        };

        var command = new Command("search", "Search the unit catalog.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);
        command.Options.Add(queryBodyOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);
            var bodyNode = new JsonObject();
            var queryValue = parseResult.GetValue(queryBodyOption)!;
            bodyNode["query"] = JsonValue.Create(queryValue);
            var bodyJson = bodyNode.ToJsonString();
            var body = await KiotaJsonSerializer.DeserializeAsync<SearchRequest>(
                bodyJson, SearchRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.UnitV3.V3.Unit.Search.PostAsync(body, configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("units", ("Name", "name"), ("Symbol", "displaySymbol"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>List every unit that uses a given symbol, across all namespaces.</summary>
    /// <remarks>GET /v3/unit/symbols on the UnitV3 service.</remarks>
    private static Command BuildUnitBySymbol()
    {
        var symbolOption = new Option<string>("--symbol", "-s")
        {
            Description = "Unit symbol, e.g. m.",
            Required = true,
        };

        var command = new Command("by-symbol", "List every unit that uses a given symbol, across all namespaces.");
        command.Options.Add(symbolOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var symbol = parseResult.GetValue(symbolOption)!;

            var result = await context.Client.UnitV3.V3.Unit.Symbols.GetAsync(configuration =>
            {
                configuration.QueryParameters.Symbol = symbol;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("units", ("Name", "name"), ("Symbol", "displaySymbol"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>List the units that can measure a given measurement.</summary>
    /// <remarks>GET /v3/unit/measurement on the UnitV3 service.</remarks>
    private static Command BuildUnitByMeasurement()
    {
        var ancestryOption = new Option<string>("--measurement", "-m")
        {
            Description = "Measurement ancestry, e.g. Length.",
            Required = true,
        };

        var command = new Command("by-measurement", "List the units that can measure a given measurement.");
        command.Options.Add(ancestryOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var ancestry = parseResult.GetValue(ancestryOption)!;

            var result = await context.Client.UnitV3.V3.Unit.Measurement.GetAsync(configuration =>
            {
                configuration.QueryParameters.Ancestry = ancestry;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("units", ("Name", "name"), ("Symbol", "displaySymbol"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Get the unit a unit system prefers for a given measurement.</summary>
    /// <remarks>GET /v3/unit/unitsystem on the UnitV3 service.</remarks>
    private static Command BuildUnitBySystem()
    {
        var unitsystemnameOption = new Option<string>("--system")
        {
            Description = "Unit system name.",
            Required = true,
        };
        var ancestryOption = new Option<string>("--measurement", "-m")
        {
            Description = "Measurement ancestry.",
            Required = true,
        };

        var command = new Command("by-system", "Get the unit a unit system prefers for a given measurement.");
        command.Options.Add(unitsystemnameOption);
        command.Options.Add(ancestryOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var unitsystemname = parseResult.GetValue(unitsystemnameOption)!;
            var ancestry = parseResult.GetValue(ancestryOption)!;

            var result = await context.Client.UnitV3.V3.Unit.Unitsystem.GetAsync(configuration =>
            {
                configuration.QueryParameters.UnitSystemName = unitsystemname;
                configuration.QueryParameters.Ancestry = ancestry;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>List the preferred units for a given measurement.</summary>
    /// <remarks>GET /v3/unit/measurement/preferred on the UnitV3 service.</remarks>
    private static Command BuildUnitPreferred()
    {
        var ancestryOption = new Option<string>("--measurement", "-m")
        {
            Description = "Measurement ancestry.",
            Required = true,
        };

        var command = new Command("preferred", "List the preferred units for a given measurement.");
        command.Options.Add(ancestryOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var ancestry = parseResult.GetValue(ancestryOption)!;

            var result = await context.Client.UnitV3.V3.Unit.Measurement.Preferred.GetAsync(configuration =>
            {
                configuration.QueryParameters.Ancestry = ancestry;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("units", ("Name", "name"), ("Symbol", "displaySymbol"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>List mappings between units in different namespaces.</summary>
    /// <remarks>GET /v3/unit/maps on the UnitV3 service.</remarks>
    private static Command BuildUnitMaps()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of mappings to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of mappings to return.",
        };

        var command = new Command("maps", "List mappings between units in different namespaces.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Unit.Maps.GetAsync(configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get the scale and offset that convert one unit to another.</summary>
    /// <remarks>GET /v3/conversion/scale on the UnitV3 service.</remarks>
    private static Command BuildUnitConversionScale()
    {
        var namespacesOption = new Option<string>("--namespace")
        {
            Description = "Namespace to resolve the symbols in, e.g. Energistics_UoM.",
            Required = true,
        };
        var fromsymbolOption = new Option<string>("--from")
        {
            Description = "Symbol to convert from, e.g. ft.",
            Required = true,
        };
        var tosymbolOption = new Option<string>("--to")
        {
            Description = "Symbol to convert to, e.g. m.",
            Required = true,
        };

        var command = new Command("scale", "Get the scale and offset that convert one unit to another.");
        command.Options.Add(namespacesOption);
        command.Options.Add(fromsymbolOption);
        command.Options.Add(tosymbolOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var namespaces = parseResult.GetValue(namespacesOption)!;
            var fromsymbol = parseResult.GetValue(fromsymbolOption)!;
            var tosymbol = parseResult.GetValue(tosymbolOption)!;

            var result = await context.Client.UnitV3.V3.Conversion.Scale.GetAsync(configuration =>
            {
                configuration.QueryParameters.Namespaces = namespaces;
                configuration.QueryParameters.FromSymbol = fromsymbol;
                configuration.QueryParameters.ToSymbol = tosymbol;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get the ABCD rational-function coefficients converting one unit to another.</summary>
    /// <remarks>GET /v3/conversion/abcd on the UnitV3 service.</remarks>
    private static Command BuildUnitConversionAbcd()
    {
        var namespacesOption = new Option<string>("--namespace")
        {
            Description = "Namespace to resolve the symbols in, e.g. Energistics_UoM.",
            Required = true,
        };
        var fromsymbolOption = new Option<string>("--from")
        {
            Description = "Symbol to convert from.",
            Required = true,
        };
        var tosymbolOption = new Option<string>("--to")
        {
            Description = "Symbol to convert to.",
            Required = true,
        };

        var command = new Command("abcd", "Get the ABCD rational-function coefficients converting one unit to another.");
        command.Options.Add(namespacesOption);
        command.Options.Add(fromsymbolOption);
        command.Options.Add(tosymbolOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var namespaces = parseResult.GetValue(namespacesOption)!;
            var fromsymbol = parseResult.GetValue(fromsymbolOption)!;
            var tosymbol = parseResult.GetValue(tosymbolOption)!;

            var result = await context.Client.UnitV3.V3.Conversion.Abcd.GetAsync(configuration =>
            {
                configuration.QueryParameters.Namespaces = namespaces;
                configuration.QueryParameters.FromSymbol = fromsymbol;
                configuration.QueryParameters.ToSymbol = tosymbol;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get the whole unit catalog.</summary>
    /// <remarks>GET /v3/catalog on the UnitV3 service.</remarks>
    private static Command BuildUnitCatalogGet()
    {

        var command = new Command("get", "Get the whole unit catalog.");

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {

            var result = await context.Client.UnitV3.V3.Catalog.GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Search across every kind of item in the catalog.</summary>
    /// <remarks>POST /v3/catalog/search on the UnitV3 service.</remarks>
    private static Command BuildUnitCatalogSearch()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of results to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of results to return.",
        };
        var queryBodyOption = new Option<string>("--query", "-q")
        {
            Description = "Search expression.",
            Required = true,
        };

        var command = new Command("search", "Search across every kind of item in the catalog.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);
        command.Options.Add(queryBodyOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);
            var bodyNode = new JsonObject();
            var queryValue = parseResult.GetValue(queryBodyOption)!;
            bodyNode["query"] = JsonValue.Create(queryValue);
            var bodyJson = bodyNode.ToJsonString();
            var body = await KiotaJsonSerializer.DeserializeAsync<SearchRequest>(
                bodyJson, SearchRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.UnitV3.V3.Catalog.Search.PostAsync(body, configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>List the states a unit or measurement mapping can be in.</summary>
    /// <remarks>GET /v3/catalog/mapstates on the UnitV3 service.</remarks>
    private static Command BuildUnitCatalogMapStates()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of states to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of states to return.",
        };

        var command = new Command("map-states", "List the states a unit or measurement mapping can be in.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Catalog.Mapstates.GetAsync(configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Show when the catalog was last changed.</summary>
    /// <remarks>GET /v3/catalog/lastmodified on the UnitV3 service.</remarks>
    private static Command BuildUnitCatalogLastModified()
    {

        var command = new Command("last-modified", "Show when the catalog was last changed.");

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {

            var result = await context.Client.UnitV3.V3.Catalog.Lastmodified.GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>List all measurements in the catalog.</summary>
    /// <remarks>GET /v3/measurement/list on the UnitV3 service.</remarks>
    private static Command BuildMeasurementList()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of measurements to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of measurements to return.",
        };

        var command = new Command("list", "List all measurements in the catalog.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Measurement.List.GetAsync(configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("measurements", ("Name", "name"), ("Ancestry", "ancestry"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Get one measurement by ancestry.</summary>
    /// <remarks>GET /v3/measurement on the UnitV3 service.</remarks>
    private static Command BuildMeasurementGet()
    {
        var ancestryOption = new Option<string>("--measurement", "-m")
        {
            Description = "Measurement ancestry, e.g. Length.",
            Required = true,
        };

        var command = new Command("get", "Get one measurement by ancestry.");
        command.Options.Add(ancestryOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var ancestry = parseResult.GetValue(ancestryOption)!;

            var result = await context.Client.UnitV3.V3.Measurement.GetAsync(configuration =>
            {
                configuration.QueryParameters.Ancestry = ancestry;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Search the measurement catalog.</summary>
    /// <remarks>POST /v3/measurement/search on the UnitV3 service.</remarks>
    private static Command BuildMeasurementSearch()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of results to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of results to return.",
        };
        var queryBodyOption = new Option<string>("--query", "-q")
        {
            Description = "Search expression.",
            Required = true,
        };

        var command = new Command("search", "Search the measurement catalog.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);
        command.Options.Add(queryBodyOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);
            var bodyNode = new JsonObject();
            var queryValue = parseResult.GetValue(queryBodyOption)!;
            bodyNode["query"] = JsonValue.Create(queryValue);
            var bodyJson = bodyNode.ToJsonString();
            var body = await KiotaJsonSerializer.DeserializeAsync<SearchRequest>(
                bodyJson, SearchRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.UnitV3.V3.Measurement.Search.PostAsync(body, configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("measurements", ("Name", "name"), ("Ancestry", "ancestry"), ("Namespace", "namespace")));
        }, cancellationToken));

        return command;
    }

    /// <summary>List mappings between measurements in different namespaces.</summary>
    /// <remarks>GET /v3/measurement/maps on the UnitV3 service.</remarks>
    private static Command BuildMeasurementMaps()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of mappings to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of mappings to return.",
        };

        var command = new Command("maps", "List mappings between measurements in different namespaces.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Measurement.Maps.GetAsync(configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>List the available unit systems.</summary>
    /// <remarks>GET /v3/unitsystem/list on the UnitV3 service.</remarks>
    private static Command BuildUnitSystemList()
    {
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of unit systems to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of unit systems to return.",
        };

        var command = new Command("list", "List the available unit systems.");
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Unitsystem.List.GetAsync(configuration =>
            {
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get one unit system by name.</summary>
    /// <remarks>GET /v3/unitsystem on the UnitV3 service.</remarks>
    private static Command BuildUnitSystemGet()
    {
        var nameOption = new Option<string>("--name", "-n")
        {
            Description = "Unit system name.",
            Required = true,
        };
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of entries to skip.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of entries to return.",
        };

        var command = new Command("get", "Get one unit system by name.");
        command.Options.Add(nameOption);
        command.Options.Add(offsetOption);
        command.Options.Add(limitOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var name = parseResult.GetValue(nameOption)!;
            var offset = parseResult.GetValue(offsetOption);
            var limit = parseResult.GetValue(limitOption);

            var result = await context.Client.UnitV3.V3.Unitsystem.GetAsync(configuration =>
            {
                configuration.QueryParameters.Name = name;
                configuration.QueryParameters.Offset = offset;
                configuration.QueryParameters.Limit = limit;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }
}
