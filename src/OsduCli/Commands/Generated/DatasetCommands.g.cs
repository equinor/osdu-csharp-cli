// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: dataset (1.0.0)
//   manifest: cli-manifest/dataset.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.Dataset.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>Dataset service — dataset registries and access instructions.</summary>
public static partial class DatasetCommands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("dataset").Subcommands.Add(BuildDatasetGet());
        tree.Node("dataset").Subcommands.Add(BuildDatasetAdd());
        tree.Node("dataset").Subcommands.Add(BuildDatasetDelete());
        tree.Node("dataset").Subcommands.Add(BuildDatasetUndelete());
        tree.Node("dataset").Subcommands.Add(BuildDatasetUploadUrl());
        tree.Node("dataset").Subcommands.Add(BuildDatasetDownloadUrl());
        tree.Node("dataset").Subcommands.Add(BuildDatasetRevokeUrl());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>Get a dataset registry record by id.</summary>
    /// <remarks>GET /getDatasetRegistry on the Dataset service.</remarks>
    private static Command BuildDatasetGet()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "Dataset registry record id.",
            Required = true,
        };

        var command = new Command("get", "Get a dataset registry record by id.");
        command.Options.Add(idOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;

            var result = await context.Client.Dataset.GetDatasetRegistry.GetAsync(configuration =>
            {
                configuration.QueryParameters.Id = id;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("datasetRegistries", ("Id", "id"), ("Kind", "kind")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Create or update dataset registry records from a JSON file.</summary>
    /// <remarks>PUT /registerDataset on the Dataset service.</remarks>
    private static Command BuildDatasetAdd()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the dataset registry records.",
            Required = true,
        };

        var command = new Command("add", "Create or update dataset registry records from a JSON file.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<CreateDatasetRegistryRequest>(
                bodyJson, CreateDatasetRegistryRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Dataset.RegisterDataset.PutAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("datasetRegistries", ("Id", "id"), ("Kind", "kind")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Soft-delete a dataset metadata record.</summary>
    /// <remarks>POST /metadataRecord/{id}/softDelete on the Dataset service.</remarks>
    private static Command BuildDatasetDelete()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "Dataset metadata record id.",
            Required = true,
        };

        var command = new Command("delete", "Soft-delete a dataset metadata record.");
        command.Options.Add(idOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;

            await context.Client.Dataset.MetadataRecord[id].SoftDelete.PostAsync(cancellationToken: cancellationToken);

            return context.Output.WriteMessage("Dataset metadata record soft-deleted");
        }, cancellationToken));

        return command;
    }

    /// <summary>Restore a soft-deleted dataset metadata record.</summary>
    /// <remarks>POST /metadataRecord/{id}/undelete on the Dataset service.</remarks>
    private static Command BuildDatasetUndelete()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "Dataset metadata record id to restore.",
            Required = true,
        };

        var command = new Command("undelete", "Restore a soft-deleted dataset metadata record.");
        command.Options.Add(idOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;

            var result = await context.Client.Dataset.MetadataRecord[id].Undelete.PostAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get instructions for writing a new dataset's bytes.</summary>
    /// <remarks>POST /storageInstructions on the Dataset service.</remarks>
    private static Command BuildDatasetUploadUrl()
    {
        var kindsubtypeOption = new Option<string>("--kind-subtype")
        {
            Description = "Dataset sub-type, e.g. dataset--File.Generic.",
            Required = true,
        };
        var expirytimeOption = new Option<string>("--expiry")
        {
            Description = "How long the instructions stay valid, e.g. 5M or 2H.",
        };

        var command = new Command("upload-url", "Get instructions for writing a new dataset's bytes.");
        command.Options.Add(kindsubtypeOption);
        command.Options.Add(expirytimeOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var kindsubtype = parseResult.GetValue(kindsubtypeOption)!;
            var expirytime = parseResult.GetValue(expirytimeOption);

            var result = await context.Client.Dataset.StorageInstructions.PostAsync(configuration =>
            {
                configuration.QueryParameters.KindSubType = kindsubtype;
                configuration.QueryParameters.ExpiryTime = expirytime;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get instructions for reading a dataset's bytes.</summary>
    /// <remarks>GET /retrievalInstructions on the Dataset service.</remarks>
    private static Command BuildDatasetDownloadUrl()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "Dataset registry record id.",
            Required = true,
        };
        var expirytimeOption = new Option<string>("--expiry")
        {
            Description = "How long the instructions stay valid, e.g. 5M or 2H.",
        };

        var command = new Command("download-url", "Get instructions for reading a dataset's bytes.");
        command.Options.Add(idOption);
        command.Options.Add(expirytimeOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;
            var expirytime = parseResult.GetValue(expirytimeOption);

            var result = await context.Client.Dataset.RetrievalInstructions.GetAsync(configuration =>
            {
                configuration.QueryParameters.Id = id;
                configuration.QueryParameters.ExpiryTime = expirytime;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Revoke previously issued dataset URLs.</summary>
    /// <remarks>POST /revokeURL on the Dataset service.</remarks>
    private static Command BuildDatasetRevokeUrl()
    {
        var kindsubtypeOption = new Option<string>("--kind-subtype")
        {
            Description = "Dataset sub-type whose URLs should be revoked.",
            Required = true,
        };
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file naming the resource whose URLs should be revoked.",
            Required = true,
        };

        var command = new Command("revoke-url", "Revoke previously issued dataset URLs.");
        command.Options.Add(kindsubtypeOption);
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var kindsubtype = parseResult.GetValue(kindsubtypeOption)!;
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<Equinor.OsduCsharpClient.Dataset.RevokeURL.RevokeURLPostRequestBody>(
                bodyJson, Equinor.OsduCsharpClient.Dataset.RevokeURL.RevokeURLPostRequestBody.CreateFromDiscriminatorValue, cancellationToken);

            await context.Client.Dataset.RevokeURL.PostAsync(body, configuration =>
            {
                configuration.QueryParameters.KindSubType = kindsubtype;
            }, cancellationToken);

            return context.Output.WriteMessage("Dataset URLs revoked");
        }, cancellationToken));

        return command;
    }
}
