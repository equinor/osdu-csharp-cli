// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: workflow (2.0.5)
//   manifest: cli-manifest/workflow.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using System.Text.Json.Nodes;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.Workflow.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>Workflow service — ingestion and processing pipelines.</summary>
public static partial class WorkflowCommands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("workflow").Subcommands.Add(BuildWorkflowList());
        tree.Node("workflow").Subcommands.Add(BuildWorkflowGet());
        tree.Node("workflow").Subcommands.Add(BuildWorkflowAdd());
        tree.Node("workflow").Subcommands.Add(BuildWorkflowDelete());
        tree.Node("workflow run").Subcommands.Add(BuildWorkflowRunList());
        tree.Node("workflow run").Subcommands.Add(BuildWorkflowRunGet());
        tree.Node("workflow run").Subcommands.Add(BuildWorkflowRunTrigger());
        tree.Node("workflow run").Subcommands.Add(BuildWorkflowRunUpdate());
        tree.Node("workflow run").Subcommands.Add(BuildWorkflowRunLatest());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>List workflows registered in the partition.</summary>
    /// <remarks>GET /v1/workflow on the Workflow service.</remarks>
    private static Command BuildWorkflowList()
    {
        var prefixOption = new Option<string>("--prefix")
        {
            Description = "Return only workflows whose name starts with this.",
        };

        var command = new Command("list", "List workflows registered in the partition.");
        command.Options.Add(prefixOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var prefix = parseResult.GetValue(prefixOption);

            var result = await context.Client.Workflow.V1.Workflow.GetAsync(configuration =>
            {
                configuration.QueryParameters.Prefix = prefix;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Name", "workflowName"), ("Description", "description"), ("CreatedBy", "createdBy")));
        }, cancellationToken, "service.workflow.viewer"));

        return command;
    }

    /// <summary>Get a workflow definition by name.</summary>
    /// <remarks>GET /v1/workflow/{workflow_name} on the Workflow service.</remarks>
    private static Command BuildWorkflowGet()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name.",
            Required = true,
        };

        var command = new Command("get", "Get a workflow definition by name.");
        command.Options.Add(workflowNameOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;

            var result = await context.Client.Workflow.V1.Workflow[workflowName].GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken, "service.workflow.viewer"));

        return command;
    }

    /// <summary>Register a workflow definition from a JSON file.</summary>
    /// <remarks>POST /v1/workflow on the Workflow service.</remarks>
    private static Command BuildWorkflowAdd()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the workflow definition.",
            Required = true,
        };

        var command = new Command("add", "Register a workflow definition from a JSON file.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<CreateWorkflowRequest>(
                bodyJson, CreateWorkflowRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Workflow.V1.Workflow.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Name", "workflowName"), ("Id", "workflowId")));
        }, cancellationToken, "service.workflow.admin"));

        return command;
    }

    /// <summary>Delete a workflow definition by name.</summary>
    /// <remarks>DELETE /v1/workflow/{workflow_name} on the Workflow service.</remarks>
    private static Command BuildWorkflowDelete()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name to delete.",
            Required = true,
        };

        var command = new Command("delete", "Delete a workflow definition by name.");
        command.Options.Add(workflowNameOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;

            await context.Client.Workflow.V1.Workflow[workflowName].DeleteAsync(cancellationToken: cancellationToken);

            return context.Output.WriteMessage("Workflow deleted");
        }, cancellationToken, "service.workflow.admin"));

        return command;
    }

    /// <summary>List the runs of a workflow.</summary>
    /// <remarks>GET /v1/workflow/{workflow_name}/workflowRun on the Workflow service.</remarks>
    private static Command BuildWorkflowRunList()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name.",
            Required = true,
        };

        var command = new Command("list", "List the runs of a workflow.");
        command.Options.Add(workflowNameOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;

            var result = await context.Client.Workflow.V1.Workflow[workflowName].WorkflowRun.GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("RunId", "runId"), ("Status", "status"), ("StartTimeStamp", "startTimeStamp")));
        }, cancellationToken, "service.workflow.viewer"));

        return command;
    }

    /// <summary>Get one workflow run.</summary>
    /// <remarks>GET /v1/workflow/{workflow_name}/workflowRun/{runId} on the Workflow service.</remarks>
    private static Command BuildWorkflowRunGet()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name.",
            Required = true,
        };
        var runidOption = new Option<string>("--run", "-r")
        {
            Description = "Run id.",
            Required = true,
        };

        var command = new Command("get", "Get one workflow run.");
        command.Options.Add(workflowNameOption);
        command.Options.Add(runidOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;
            var runid = parseResult.GetValue(runidOption)!;

            var result = await context.Client.Workflow.V1.Workflow[workflowName].WorkflowRun[runid].GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken, "service.workflow.viewer"));

        return command;
    }

    /// <summary>Start a new run of a workflow.</summary>
    /// <remarks>POST /v1/workflow/{workflow_name}/workflowRun on the Workflow service.</remarks>
    private static Command BuildWorkflowRunTrigger()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name to run.",
            Required = true,
        };
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the run request, including its executionContext.",
            Required = true,
        };

        var command = new Command("trigger", "Start a new run of a workflow.");
        command.Options.Add(workflowNameOption);
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<TriggerWorkflowRequest>(
                bodyJson, TriggerWorkflowRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Workflow.V1.Workflow[workflowName].WorkflowRun.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("RunId", "runId"), ("Status", "status")));
        }, cancellationToken, "service.workflow.creator"));

        return command;
    }

    /// <summary>Update the status of a workflow run.</summary>
    /// <remarks>PUT /v1/workflow/{workflow_name}/workflowRun/{runId} on the Workflow service.</remarks>
    private static Command BuildWorkflowRunUpdate()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name.",
            Required = true,
        };
        var runidOption = new Option<string>("--run", "-r")
        {
            Description = "Run id.",
            Required = true,
        };
        var statusBodyOption = new Option<string>("--status")
        {
            Description = "New status for the run. One of: submitted, running, finished, failed, success, queued.",
            Required = true,
        };
        statusBodyOption.AcceptOnlyFromAmong("submitted", "running", "finished", "failed", "success", "queued");

        var command = new Command("update", "Update the status of a workflow run.");
        command.Options.Add(workflowNameOption);
        command.Options.Add(runidOption);
        command.Options.Add(statusBodyOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;
            var runid = parseResult.GetValue(runidOption)!;
            var bodyNode = new JsonObject();
            var statusValue = parseResult.GetValue(statusBodyOption)!;
            bodyNode["status"] = JsonValue.Create(statusValue);
            var bodyJson = bodyNode.ToJsonString();
            var body = await KiotaJsonSerializer.DeserializeAsync<UpdateWorkflowRunRequest>(
                bodyJson, UpdateWorkflowRunRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.Workflow.V1.Workflow[workflowName].WorkflowRun[runid].PutAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("RunId", "runId"), ("Status", "status")));
        }, cancellationToken, "service.workflow.viewer"));

        return command;
    }

    /// <summary>Show execution detail for the most recent task of a run.</summary>
    /// <remarks>GET /v1/workflow/{workflow_name}/workflowRun/{runId}/latestInfo on the Workflow service.</remarks>
    private static Command BuildWorkflowRunLatest()
    {
        var workflowNameOption = new Option<string>("--name", "-n")
        {
            Description = "Workflow name.",
            Required = true,
        };
        var runidOption = new Option<string>("--run", "-r")
        {
            Description = "Run id.",
            Required = true,
        };

        var command = new Command("latest", "Show execution detail for the most recent task of a run.");
        command.Options.Add(workflowNameOption);
        command.Options.Add(runidOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var workflowName = parseResult.GetValue(workflowNameOption)!;
            var runid = parseResult.GetValue(runidOption)!;

            var result = await context.Client.Workflow.V1.Workflow[workflowName].WorkflowRun[runid].LatestInfo.GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken, "service.workflow.viewer"));

        return command;
    }
}
