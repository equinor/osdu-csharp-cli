// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: file (2.0.0)
//   manifest: cli-manifest/file.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.FileNamespace.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>File service — upload, download and file metadata.</summary>
public static partial class FileCommands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("file").Subcommands.Add(BuildFileGet());
        tree.Node("file").Subcommands.Add(BuildFileAdd());
        tree.Node("file").Subcommands.Add(BuildFileDelete());
        tree.Node("file").Subcommands.Add(BuildFileUploadUrl());
        tree.Node("file").Subcommands.Add(BuildFileDownloadUrl());
        tree.Node("file").Subcommands.Add(BuildFileRevokeUrl());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>Get the metadata record for a file.</summary>
    /// <remarks>GET /v2/files/{id}/metadata on the File service.</remarks>
    private static Command BuildFileGet()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "File metadata record id.",
            Required = true,
        };

        var command = new Command("get", "Get the metadata record for a file.");
        command.Options.Add(idOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;

            var result = await context.Client.File.V2.Files[id].Metadata.GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Register metadata for a file already uploaded to the landing zone.</summary>
    /// <remarks>POST /v2/files/metadata on the File service.</remarks>
    private static Command BuildFileAdd()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the file metadata record.",
            Required = true,
        };

        var command = new Command("add", "Register metadata for a file already uploaded to the landing zone.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<FileMetadata>(
                bodyJson, FileMetadata.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.File.V2.Files.Metadata.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Id", "id")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Delete a file and its metadata record.</summary>
    /// <remarks>DELETE /v2/files/{id}/metadata on the File service.</remarks>
    private static Command BuildFileDelete()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "File metadata record id to delete.",
            Required = true,
        };

        var command = new Command("delete", "Delete a file and its metadata record.");
        command.Options.Add(idOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;

            await context.Client.File.V2.Files[id].Metadata.DeleteAsync(cancellationToken: cancellationToken);

            return context.Output.WriteMessage("File and metadata deleted");
        }, cancellationToken));

        return command;
    }

    /// <summary>Get a signed URL and landing-zone location to upload a file to.</summary>
    /// <remarks>GET /v2/files/uploadURL on the File service.</remarks>
    private static Command BuildFileUploadUrl()
    {
        var expirytimeOption = new Option<string>("--expiry")
        {
            Description = "How long the URL stays valid, e.g. 5M for five minutes or 2H for two hours.",
        };

        var command = new Command("upload-url", "Get a signed URL and landing-zone location to upload a file to.");
        command.Options.Add(expirytimeOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var expirytime = parseResult.GetValue(expirytimeOption);

            var result = await context.Client.File.V2.Files.UploadURL.GetAsync(configuration =>
            {
                configuration.QueryParameters.ExpiryTime = expirytime;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get a signed URL to download a file from.</summary>
    /// <remarks>GET /v2/files/{id}/downloadURL on the File service.</remarks>
    private static Command BuildFileDownloadUrl()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "File metadata record id.",
            Required = true,
        };
        var expirytimeOption = new Option<string>("--expiry")
        {
            Description = "How long the URL stays valid, e.g. 5M for five minutes or 2H for two hours.",
        };

        var command = new Command("download-url", "Get a signed URL to download a file from.");
        command.Options.Add(idOption);
        command.Options.Add(expirytimeOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;
            var expirytime = parseResult.GetValue(expirytimeOption);

            var result = await context.Client.File.V2.Files[id].DownloadURL.GetAsync(configuration =>
            {
                configuration.QueryParameters.ExpiryTime = expirytime;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Revoke previously issued signed URLs.</summary>
    /// <remarks>POST /v2/files/revokeURL on the File service.</remarks>
    private static Command BuildFileRevokeUrl()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file naming the resource whose URLs should be revoked.",
            Required = true,
        };

        var command = new Command("revoke-url", "Revoke previously issued signed URLs.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<Equinor.OsduCsharpClient.FileNamespace.V2.Files.RevokeURL.RevokeURLPostRequestBody>(
                bodyJson, Equinor.OsduCsharpClient.FileNamespace.V2.Files.RevokeURL.RevokeURLPostRequestBody.CreateFromDiscriminatorValue, cancellationToken);

            await context.Client.File.V2.Files.RevokeURL.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.WriteMessage("Signed URLs revoked");
        }, cancellationToken));

        return command;
    }
}
