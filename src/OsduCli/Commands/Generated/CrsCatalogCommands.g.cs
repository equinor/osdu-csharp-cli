// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: crs_catalog (3.0.0)
//   manifest: cli-manifest/crs_catalog.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using System.CommandLine.Parsing;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.CrsCatalog.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>CRS Catalog service — coordinate reference system and transformation lookups.</summary>
public static partial class CrsCatalogCommands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("crs").Subcommands.Add(BuildCrsGet());
        tree.Node("crs").Subcommands.Add(BuildCrsTransform());
        tree.Node("crs").Subcommands.Add(BuildCrsAreaOfUse());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>Get a coordinate reference system by record or data id.</summary>
    /// <remarks>GET /v3/coordinate-reference-system on the CrsCatalog service.</remarks>
    private static Command BuildCrsGet()
    {
        var recordidOption = new Option<string>("--record-id")
        {
            Description = "CRS record id.",
        };
        var dataidOption = new Option<string>("--data-id")
        {
            Description = "CRS data id. Give this or --record-id.",
        };

        var command = new Command("get", "Get a coordinate reference system by record or data id.");
        command.Options.Add(recordidOption);
        command.Options.Add(dataidOption);

        // A parse-time validator, so this costs no config load, no
        // token and no round trip. The service rejects the request
        // anyway; it should not have to.
        command.Validators.Add(result =>
        {
            if (result.GetResult(recordidOption) is null && result.GetResult(dataidOption) is null)
                result.AddError("One of --record-id or --data-id is required.");
        });

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var recordid = parseResult.GetValue(recordidOption);
            var dataid = parseResult.GetValue(dataidOption);

            var result = await context.Client.CrsCatalog.V3.CoordinateReferenceSystem.GetAsync(configuration =>
            {
                configuration.QueryParameters.RecordId = recordid;
                configuration.QueryParameters.DataId = dataid;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Get a coordinate transformation by record or data id.</summary>
    /// <remarks>GET /v3/coordinate-transformation on the CrsCatalog service.</remarks>
    private static Command BuildCrsTransform()
    {
        var recordidOption = new Option<string>("--record-id")
        {
            Description = "Transformation record id.",
        };
        var dataidOption = new Option<string>("--data-id")
        {
            Description = "Transformation data id. Give this or --record-id.",
        };

        var command = new Command("transform", "Get a coordinate transformation by record or data id.");
        command.Options.Add(recordidOption);
        command.Options.Add(dataidOption);

        // A parse-time validator, so this costs no config load, no
        // token and no round trip. The service rejects the request
        // anyway; it should not have to.
        command.Validators.Add(result =>
        {
            if (result.GetResult(recordidOption) is null && result.GetResult(dataidOption) is null)
                result.AddError("One of --record-id or --data-id is required.");
        });

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var recordid = parseResult.GetValue(recordidOption);
            var dataid = parseResult.GetValue(dataidOption);

            var result = await context.Client.CrsCatalog.V3.CoordinateTransformation.GetAsync(configuration =>
            {
                configuration.QueryParameters.RecordId = recordid;
                configuration.QueryParameters.DataId = dataid;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Check which transformations are valid for a set of points.</summary>
    /// <remarks>POST /v3/points-in-aou on the CrsCatalog service.</remarks>
    private static Command BuildCrsAreaOfUse()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the points and the transformations to test.",
            Required = true,
        };

        var command = new Command("area-of-use", "Check which transformations are valid for a set of points.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<InPolygonQuery>(
                bodyJson, InPolygonQuery.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.CrsCatalog.V3.PointsInAou.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }
}
