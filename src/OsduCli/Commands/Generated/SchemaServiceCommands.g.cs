// <auto-generated/>
//
// Generated by tools/generate_cli.py from:
//   spec: schema_service (1.0)
//   manifest: cli-manifest/schema_service.yaml
//
// Do not edit. Behaviour that the manifest can express belongs in the manifest; anything
// it cannot belongs in a partial class under Commands/Handwritten/ via the Customize hook.

using System.CommandLine;
using Microsoft.Kiota.Abstractions.Serialization;
using Equinor.OsduCli.Runtime;
using Equinor.OsduCsharpClient.SchemaService.Models;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>Schema service — the definitions behind every record kind.</summary>
public static partial class SchemaServiceCommands
{
    /// <summary>Attaches this service's commands to the global tree.</summary>
    public static void Attach(CommandTree tree)
    {
        tree.Node("schema").Subcommands.Add(BuildSchemaList());
        tree.Node("schema").Subcommands.Add(BuildSchemaGet());
        tree.Node("schema").Subcommands.Add(BuildSchemaAdd());
        tree.Node("schema").Subcommands.Add(BuildSchemaUpdate());

        Customize(tree);
    }

    /// <summary>
    /// Implemented in Commands/Handwritten/ to add commands this manifest cannot
    /// express, or to adjust generated ones. No-op when nothing implements it.
    /// </summary>
    static partial void Customize(CommandTree tree);

    /// <summary>List schemas, optionally filtered by authority, source or entity type.</summary>
    /// <remarks>GET /schema on the SchemaService service.</remarks>
    private static Command BuildSchemaList()
    {
        var authorityOption = new Option<string>("--authority")
        {
            Description = "Filter by authority, e.g. osdu.",
        };
        var sourceOption = new Option<string>("--source")
        {
            Description = "Filter by source, e.g. wks.",
        };
        var entitytypeOption = new Option<string>("--entity-type", "-e")
        {
            Description = "Filter by entity type, e.g. master-data--Well.",
        };
        var statusOption = new Option<string>("--status")
        {
            Description = "Filter by lifecycle status: PUBLISHED, DEVELOPMENT or OBSOLETE.",
        };
        var scopeOption = new Option<string>("--scope")
        {
            Description = "Filter by scope: SHARED or INTERNAL.",
        };
        var latestversionOption = new Option<bool>("--latest")
        {
            Description = "Return only the newest version of each schema.",
        };
        var limitOption = new Option<int?>("--limit")
        {
            Description = "Maximum number of schemas to return.",
        };
        var offsetOption = new Option<int?>("--offset")
        {
            Description = "Number of schemas to skip.",
        };

        var command = new Command("list", "List schemas, optionally filtered by authority, source or entity type.");
        command.Options.Add(authorityOption);
        command.Options.Add(sourceOption);
        command.Options.Add(entitytypeOption);
        command.Options.Add(statusOption);
        command.Options.Add(scopeOption);
        command.Options.Add(latestversionOption);
        command.Options.Add(limitOption);
        command.Options.Add(offsetOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var authority = parseResult.GetValue(authorityOption);
            var source = parseResult.GetValue(sourceOption);
            var entitytype = parseResult.GetValue(entitytypeOption);
            var status = parseResult.GetValue(statusOption);
            var scope = parseResult.GetValue(scopeOption);
            var latestversion = parseResult.GetValue(latestversionOption);
            var limit = parseResult.GetValue(limitOption);
            var offset = parseResult.GetValue(offsetOption);

            var result = await context.Client.SchemaService.Schema.GetAsync(configuration =>
            {
                configuration.QueryParameters.Authority = authority;
                configuration.QueryParameters.Source = source;
                configuration.QueryParameters.EntityType = entitytype;
                configuration.QueryParameters.Status = status;
                configuration.QueryParameters.Scope = scope;
                configuration.QueryParameters.LatestVersion = latestversion;
                configuration.QueryParameters.Limit = limit;
                configuration.QueryParameters.Offset = offset;
            }, cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table("schemaInfos", ("Id", "schemaIdentity.id"), ("Status", "status"), ("Scope", "scope")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Get a schema by id.</summary>
    /// <remarks>GET /schema/{id} on the SchemaService service.</remarks>
    private static Command BuildSchemaGet()
    {
        var idOption = new Option<string>("--id", "-id")
        {
            Description = "Schema id, e.g. osdu:wks:master-data--Well:1.0.0.",
            Required = true,
        };

        var command = new Command("get", "Get a schema by id.");
        command.Options.Add(idOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var id = parseResult.GetValue(idOption)!;

            var result = await context.Client.SchemaService.Schema[id].GetAsync(cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Raw);
        }, cancellationToken));

        return command;
    }

    /// <summary>Create a schema from a JSON file.</summary>
    /// <remarks>POST /schema on the SchemaService service.</remarks>
    private static Command BuildSchemaAdd()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the schema request (schemaInfo and schema).",
            Required = true,
        };

        var command = new Command("add", "Create a schema from a JSON file.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<SchemaRequest>(
                bodyJson, SchemaRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.SchemaService.Schema.PostAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Id", "schemaIdentity.id"), ("Status", "status")));
        }, cancellationToken));

        return command;
    }

    /// <summary>Create or replace a schema from a JSON file.</summary>
    /// <remarks>PUT /schema on the SchemaService service.</remarks>
    private static Command BuildSchemaUpdate()
    {
        var fileOption = new Option<string>("--file", "-f")
        {
            Description = "JSON file containing the schema request (schemaInfo and schema).",
            Required = true,
        };

        var command = new Command("update", "Create or replace a schema from a JSON file.");
        command.Options.Add(fileOption);

        command.SetAction((parseResult, cancellationToken) =>
            CliRunner.RunAsync(parseResult, async (context, cancellationToken) =>
        {
            var bodyJson = await CliContext.ReadBodyFileAsync(parseResult.GetValue(fileOption)!, cancellationToken);
            var body = await KiotaJsonSerializer.DeserializeAsync<SchemaRequest>(
                bodyJson, SchemaRequest.CreateFromDiscriminatorValue, cancellationToken);

            var result = await context.Client.SchemaService.Schema.PutAsync(body, cancellationToken: cancellationToken);

            return context.Output.Write(
                await OsduJson.ToJsonAsync(result),
                OutputSpec.Table(null, ("Id", "schemaIdentity.id"), ("Status", "status")));
        }, cancellationToken));

        return command;
    }
}
