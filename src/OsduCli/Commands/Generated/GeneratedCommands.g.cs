// <auto-generated/>
//
// Generated by tools/generate_cli.py. Assembles the global command tree.
//
// The tree is built here rather than by each service because commands are named for
// the resource, not the service: Entitlements contributes both `group` and `member`,
// while `crs` is contributed by two services. See Runtime/CommandTree.cs.

using System.CommandLine;
using Equinor.OsduCli.Runtime;

namespace Equinor.OsduCli.Commands.Generated;

/// <summary>Every generated command, assembled into one tree.</summary>
public static class GeneratedCommands
{
    public static IReadOnlyList<Command> All()
    {
        var tree = new CommandTree();

        tree.Describe("crs", "Work with coordinate reference systems — look them up and convert between them.");
        tree.Describe("dataset", "Work with datasets — registry records and the instructions to read or write their bytes.");
        tree.Describe("group", "Work with entitlement groups — the access control lists of OSDU.");
        tree.Describe("member", "Work with members — the users and service principals inside groups.");
        tree.Describe("file", "Work with files — upload and download bytes, and the metadata that describes them.");
        tree.Describe("legaltag", "Work with LegalTags — the compliance metadata attached to every record.");
        tree.Describe("schema", "Work with schemas — the definitions behind every record kind.");
        tree.Describe("record", "Work with records — the generic, kind-agnostic unit of OSDU data.");
        tree.Describe("unit", "Work with units of measure — look them up and derive conversions between them.");
        tree.Describe("measurement", "Work with measurements — the quantities that units measure.");
        tree.Describe("unit-system", "Work with unit systems — named sets of preferred units.");
        tree.Describe("wellbore", "Manage Wellbore records in the Wellbore DDMS.");
        tree.Describe("workflow", "Work with workflows — the ingestion and processing pipelines of OSDU.");
        tree.Describe("group member", "Manage who belongs to a group.");
        tree.Describe("member group", "Inspect the groups a member belongs to.");
        tree.Describe("record version", "Inspect record version history.");
        tree.Describe("unit conversion", "Derive the arithmetic that converts one unit to another.");
        tree.Describe("unit catalog", "Inspect the unit catalog as a whole.");
        tree.Describe("wellbore version", "Inspect Wellbore record version history.");
        tree.Describe("workflow run", "Inspect and manage individual workflow executions.");

        tree.Node("crs");
        tree.Node("dataset");
        tree.Node("group");
        tree.Node("member");
        tree.Node("file");
        tree.Node("legaltag");
        tree.Node("schema");
        tree.Node("record");
        tree.Node("unit");
        tree.Node("measurement");
        tree.Node("unit-system");
        tree.Node("wellbore");
        tree.Node("workflow");
        tree.Node("group member");
        tree.Node("member group");
        tree.Node("record version");
        tree.Node("unit conversion");
        tree.Node("unit catalog");
        tree.Node("wellbore version");
        tree.Node("workflow run");

        CrsCatalogCommands.Attach(tree);
        CrsConversionCommands.Attach(tree);
        DatasetCommands.Attach(tree);
        EntitlementsCommands.Attach(tree);
        FileCommands.Attach(tree);
        LegalCommands.Attach(tree);
        SchemaServiceCommands.Attach(tree);
        SearchCommands.Attach(tree);
        StorageCommands.Attach(tree);
        UnitV3Commands.Attach(tree);
        WellboreDdmsCommands.Attach(tree);
        WorkflowCommands.Attach(tree);

        return tree.Roots;
    }
}
