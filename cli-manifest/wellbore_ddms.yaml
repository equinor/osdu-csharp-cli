# CLI manifest for the Wellbore DDMS service.
#
# Scope: every typed record resource, plus reading their bulk data. The nine resource types
# share one record shape — create, get, soft-delete, list versions, get a version — so most
# of this manifest is nine copies of five commands, and the regularity is the point: a user
# who learns `welllog get` already knows `trajectory get`.
#
# The four types that carry bulk data (welllog, trajectory, ppfg, pressuretest) add reads of
# it: `<noun> data get` for the latest, `<noun> version data` for a specific version. These
# are ordinary paginated queries — offset, limit, column selection, row filters — and map
# onto the manifest vocabulary with nothing new.
#
# Writing bulk data is still excluded, and that is a considered line rather than a to-do:
#
#   * `POST .../data` replaces a whole curve set in one call, and takes JSON or Parquet.
#   * The `sessions` endpoints are a five-call transactional protocol — open a session, send
#     chunks, patch it to commit or abandon it — which is a stateful workflow, not a command.
#
# Both are listed individually under `exclude:` rather than left out of `scope:`, so a new
# bulk endpoint upstream fails the build as `missing` instead of passing as untriaged.
#
# Also outside the scope: `/about` and `/version` (service identity, which R5 puts in
# `osducs status`) and `/log-recognition/*` (catalog admin, not a record resource).
#
# One parameter is knowingly not exposed: `DELETE` on the four bulk-carrying types accepts a
# `purge` query flag, default false, which the spec documents with an empty description. The
# delete commands here are all soft deletes. Inferring an irreversible flag's behaviour from
# its name, with no confirmation guard in place (R6 is not implemented), is not a trade worth
# making.
#
# Bulk reads return JSON here. The endpoints also offer `application/x-parquet` by content
# negotiation, which would need binary output plumbing the CLI does not have yet.

service: wellbore_ddms
spec: wellbore_ddms
client: WellboreDdms
description: Wellbore DDMS — typed wellbore records.

# Nine typed resources would otherwise be nine of the twenty-two entries on the root help
# page, crowding out `record`, `schema` and `search`. They get their own section.
section: Wellbore DDMS

# Every typed resource under /ddms/v3. The bulk write operations inside these prefixes are
# claimed by `exclude:` below, not left unscoped.
scope:
  - "/ddms/v3/*"

groups:
  well: Manage Well records.
  well version: Inspect Well record version history.
  wellbore: Manage Wellbore records.
  wellbore version: Inspect Wellbore record version history.
  welllog: Manage WellLog records.
  welllog version: Inspect WellLog record version history.
  welllog data: Read WellLog bulk data.
  trajectory: Manage WellboreTrajectory records.
  trajectory version: Inspect WellboreTrajectory record version history.
  trajectory data: Read WellboreTrajectory bulk data.
  markerset: Manage WellboreMarkerSet records.
  markerset version: Inspect WellboreMarkerSet record version history.
  intervalset: Manage WellboreIntervalSet records.
  intervalset version: Inspect WellboreIntervalSet record version history.
  logacquisition: Manage WellLogAcquisition records.
  logacquisition version: Inspect WellLogAcquisition record version history.
  ppfg: Manage PPFGDataset records.
  ppfg version: Inspect PPFGDataset record version history.
  ppfg data: Read PPFGDataset bulk data.
  pressuretest: Manage WellPressureTestRawMeasurement records.
  pressuretest version: Inspect WellPressureTestRawMeasurement record version history.
  pressuretest data: Read WellPressureTestRawMeasurement bulk data.

commands:

  # ---- Well ----------------------------------------------------------------
  - command: well get
    summary: Get a Well record by id.
    op: { method: get, path: "/ddms/v3/wells/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Well record id. }
    output: raw

  - command: well add
    summary: Add a Well record from a JSON file.
    op: { method: post, path: "/ddms/v3/wells" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the Well record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: well delete
    summary: Soft-delete a Well record by id.
    op: { method: delete, path: "/ddms/v3/wells/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Well record id to delete. }
    output:
      message: 1 record deleted

  - command: well version list
    summary: List all versions of a Well record.
    op: { method: get, path: "/ddms/v3/wells/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Well record id. }
    output: raw

  - command: well version get
    summary: Get a specific version of a Well record.
    op: { method: get, path: "/ddms/v3/wells/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Well record id. }
      version:   { flag: --version, short: -v, required: true, help: Well record version. }
    output: raw

  # ---- Wellbore ----------------------------------------------------------------
  - command: wellbore get
    summary: Get a Wellbore record by id.
    op: { method: get, path: "/ddms/v3/wellbores/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Wellbore record id. }
    output: raw

  - command: wellbore add
    summary: Add a Wellbore record from a JSON file.
    op: { method: post, path: "/ddms/v3/wellbores" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the Wellbore record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: wellbore delete
    summary: Soft-delete a Wellbore record by id.
    op: { method: delete, path: "/ddms/v3/wellbores/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Wellbore record id to delete. }
    output:
      message: 1 record deleted

  - command: wellbore version list
    summary: List all versions of a Wellbore record.
    op: { method: get, path: "/ddms/v3/wellbores/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Wellbore record id. }
    output: raw

  - command: wellbore version get
    summary: Get a specific version of a Wellbore record.
    op: { method: get, path: "/ddms/v3/wellbores/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: Wellbore record id. }
      version:   { flag: --version, short: -v, required: true, help: Wellbore record version. }
    output: raw

  # ---- WellLog ----------------------------------------------------------------
  - command: welllog get
    summary: Get a WellLog record by id.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
    output: raw

  - command: welllog add
    summary: Add a WellLog record from a JSON file.
    op: { method: post, path: "/ddms/v3/welllogs" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the WellLog record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: welllog delete
    summary: Soft-delete a WellLog record by id.
    op: { method: delete, path: "/ddms/v3/welllogs/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id to delete. }
    output:
      message: 1 record deleted

  - command: welllog version list
    summary: List all versions of a WellLog record.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
    output: raw

  - command: welllog version get
    summary: Get a specific version of a WellLog record.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
      version:   { flag: --version, short: -v, required: true, help: WellLog record version. }
    output: raw

  - command: welllog data get
    summary: Read the bulk data of a WellLog record.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  - command: welllog version data
    summary: Read the bulk data of a specific WellLog version.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}/versions/{version}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
      version:   { flag: --version, short: -v, required: true, help: WellLog record version. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  - command: welllog data stats
    summary: Summary statistics for the curves of a WellLog record.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}/data/statistics" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
      curves:    { flag: --curves, help: "Comma-separated columns, e.g. MD,GR. Omit for all." }
    output: raw

  - command: welllog version stats
    summary: Summary statistics for the curves of a specific WellLog version.
    op: { method: get, path: "/ddms/v3/welllogs/{record_id}/versions/{version}/data/statistics" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLog record id. }
      version:   { flag: --version, short: -v, required: true, help: WellLog record version. }
      curves:    { flag: --curves, help: "Comma-separated columns, e.g. MD,GR. Omit for all." }
    output: raw

  # ---- WellboreTrajectory ----------------------------------------------------------------
  - command: trajectory get
    summary: Get a WellboreTrajectory record by id.
    op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreTrajectory record id. }
    output: raw

  - command: trajectory add
    summary: Add a WellboreTrajectory record from a JSON file.
    op: { method: post, path: "/ddms/v3/wellboretrajectories" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the WellboreTrajectory record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: trajectory delete
    summary: Soft-delete a WellboreTrajectory record by id.
    op: { method: delete, path: "/ddms/v3/wellboretrajectories/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreTrajectory record id to delete. }
    output:
      message: 1 record deleted

  - command: trajectory version list
    summary: List all versions of a WellboreTrajectory record.
    op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreTrajectory record id. }
    output: raw

  - command: trajectory version get
    summary: Get a specific version of a WellboreTrajectory record.
    op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreTrajectory record id. }
      version:   { flag: --version, short: -v, required: true, help: WellboreTrajectory record version. }
    output: raw

  - command: trajectory data get
    summary: Read the bulk data of a WellboreTrajectory record.
    op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreTrajectory record id. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  - command: trajectory version data
    summary: Read the bulk data of a specific WellboreTrajectory version.
    op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}/versions/{version}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreTrajectory record id. }
      version:   { flag: --version, short: -v, required: true, help: WellboreTrajectory record version. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  # ---- WellboreMarkerSet ----------------------------------------------------------------
  - command: markerset get
    summary: Get a WellboreMarkerSet record by id.
    op: { method: get, path: "/ddms/v3/wellboremarkersets/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreMarkerSet record id. }
    output: raw

  - command: markerset add
    summary: Add a WellboreMarkerSet record from a JSON file.
    op: { method: post, path: "/ddms/v3/wellboremarkersets" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the WellboreMarkerSet record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: markerset delete
    summary: Soft-delete a WellboreMarkerSet record by id.
    op: { method: delete, path: "/ddms/v3/wellboremarkersets/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreMarkerSet record id to delete. }
    output:
      message: 1 record deleted

  - command: markerset version list
    summary: List all versions of a WellboreMarkerSet record.
    op: { method: get, path: "/ddms/v3/wellboremarkersets/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreMarkerSet record id. }
    output: raw

  - command: markerset version get
    summary: Get a specific version of a WellboreMarkerSet record.
    op: { method: get, path: "/ddms/v3/wellboremarkersets/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreMarkerSet record id. }
      version:   { flag: --version, short: -v, required: true, help: WellboreMarkerSet record version. }
    output: raw

  # ---- WellboreIntervalSet ----------------------------------------------------------------
  - command: intervalset get
    summary: Get a WellboreIntervalSet record by id.
    op: { method: get, path: "/ddms/v3/wellboreintervalsets/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreIntervalSet record id. }
    output: raw

  - command: intervalset add
    summary: Add a WellboreIntervalSet record from a JSON file.
    op: { method: post, path: "/ddms/v3/wellboreintervalsets" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the WellboreIntervalSet record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: intervalset delete
    summary: Soft-delete a WellboreIntervalSet record by id.
    op: { method: delete, path: "/ddms/v3/wellboreintervalsets/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreIntervalSet record id to delete. }
    output:
      message: 1 record deleted

  - command: intervalset version list
    summary: List all versions of a WellboreIntervalSet record.
    op: { method: get, path: "/ddms/v3/wellboreintervalsets/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreIntervalSet record id. }
    output: raw

  - command: intervalset version get
    summary: Get a specific version of a WellboreIntervalSet record.
    op: { method: get, path: "/ddms/v3/wellboreintervalsets/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellboreIntervalSet record id. }
      version:   { flag: --version, short: -v, required: true, help: WellboreIntervalSet record version. }
    output: raw

  # ---- WellLogAcquisition ----------------------------------------------------------------
  - command: logacquisition get
    summary: Get a WellLogAcquisition record by id.
    op: { method: get, path: "/ddms/v3/welllogacquisition/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLogAcquisition record id. }
    output: raw

  - command: logacquisition add
    summary: Add a WellLogAcquisition record from a JSON file.
    op: { method: post, path: "/ddms/v3/welllogacquisition" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the WellLogAcquisition record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: logacquisition delete
    summary: Soft-delete a WellLogAcquisition record by id.
    op: { method: delete, path: "/ddms/v3/welllogacquisition/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLogAcquisition record id to delete. }
    output:
      message: 1 record deleted

  - command: logacquisition version list
    summary: List all versions of a WellLogAcquisition record.
    op: { method: get, path: "/ddms/v3/welllogacquisition/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLogAcquisition record id. }
    output: raw

  - command: logacquisition version get
    summary: Get a specific version of a WellLogAcquisition record.
    op: { method: get, path: "/ddms/v3/welllogacquisition/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellLogAcquisition record id. }
      version:   { flag: --version, short: -v, required: true, help: WellLogAcquisition record version. }
    output: raw

  # ---- PPFGDataset ----------------------------------------------------------------
  - command: ppfg get
    summary: Get a PPFGDataset record by id.
    op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: PPFGDataset record id. }
    output: raw

  - command: ppfg add
    summary: Add a PPFGDataset record from a JSON file.
    op: { method: post, path: "/ddms/v3/ppfgdataset" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the PPFGDataset record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: ppfg delete
    summary: Soft-delete a PPFGDataset record by id.
    op: { method: delete, path: "/ddms/v3/ppfgdataset/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: PPFGDataset record id to delete. }
    output:
      message: 1 record deleted

  - command: ppfg version list
    summary: List all versions of a PPFGDataset record.
    op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: PPFGDataset record id. }
    output: raw

  - command: ppfg version get
    summary: Get a specific version of a PPFGDataset record.
    op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: PPFGDataset record id. }
      version:   { flag: --version, short: -v, required: true, help: PPFGDataset record version. }
    output: raw

  - command: ppfg data get
    summary: Read the bulk data of a PPFGDataset record.
    op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: PPFGDataset record id. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  - command: ppfg version data
    summary: Read the bulk data of a specific PPFGDataset version.
    op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}/versions/{version}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: PPFGDataset record id. }
      version:   { flag: --version, short: -v, required: true, help: PPFGDataset record version. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  # ---- WellPressureTestRawMeasurement ----------------------------------------------------------------
  - command: pressuretest get
    summary: Get a WellPressureTestRawMeasurement record by id.
    op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellPressureTestRawMeasurement record id. }
    output: raw

  - command: pressuretest add
    summary: Add a WellPressureTestRawMeasurement record from a JSON file.
    op: { method: post, path: "/ddms/v3/wellpressuretestrawmeasurement" }
    body:
      flag: --file
      short: -f
      required: true
      help: JSON file containing the WellPressureTestRawMeasurement record.
      # The endpoint takes an array; users pass a single record file.
      wrap-single: true
    output:
      root: recordIds

  - command: pressuretest delete
    summary: Soft-delete a WellPressureTestRawMeasurement record by id.
    op: { method: delete, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellPressureTestRawMeasurement record id to delete. }
    output:
      message: 1 record deleted

  - command: pressuretest version list
    summary: List all versions of a WellPressureTestRawMeasurement record.
    op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/versions" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellPressureTestRawMeasurement record id. }
    output: raw

  - command: pressuretest version get
    summary: Get a specific version of a WellPressureTestRawMeasurement record.
    op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/versions/{version}" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellPressureTestRawMeasurement record id. }
      version:   { flag: --version, short: -v, required: true, help: WellPressureTestRawMeasurement record version. }
    output: raw

  - command: pressuretest data get
    summary: Read the bulk data of a WellPressureTestRawMeasurement record.
    op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellPressureTestRawMeasurement record id. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

  - command: pressuretest version data
    summary: Read the bulk data of a specific WellPressureTestRawMeasurement version.
    op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/versions/{version}/data" }
    params:
      record_id: { flag: --id, short: -id, required: true, help: WellPressureTestRawMeasurement record id. }
      version:   { flag: --version, short: -v, required: true, help: WellPressureTestRawMeasurement record version. }
      offset:    { flag: --offset, help: Row to start at. }
      limit:     { flag: --limit, short: -l, help: Maximum rows to return. }
      curves:    { flag: --curves, help: "Comma-separated columns to return, e.g. MD,GR. Omit for all." }
      filter:    { flag: --filter, help: "Row filter as column:operator:value, e.g. MD:gte:1000. Operators: lt, lte, gt, gte, eq, neq." }
      describe:  { flag: --describe, help: Return row and column counts instead of the data. }
      orient:    { flag: --orient, help: JSON shape of the response. }
    output: raw

handwritten: []

exclude:
  - op: { method: post, path: "/ddms/v3/ppfgdataset/{record_id}/data" }
    reason: Replaces a record's entire bulk payload in one call, JSON or Parquet. Excluded with the rest of bulk write.
  - op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/ppfgdataset/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: get, path: "/ddms/v3/ppfgdataset/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: patch, path: "/ddms/v3/ppfgdataset/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/ppfgdataset/{record_id}/sessions/{session_id}/data" }
    reason: Chunked bulk upload; a step inside the session protocol, not a command.
  - op: { method: post, path: "/ddms/v3/wellboretrajectories/{record_id}/data" }
    reason: Replaces a record's entire bulk payload in one call, JSON or Parquet. Excluded with the rest of bulk write.
  - op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/wellboretrajectories/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: get, path: "/ddms/v3/wellboretrajectories/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: patch, path: "/ddms/v3/wellboretrajectories/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/wellboretrajectories/{record_id}/sessions/{session_id}/data" }
    reason: Chunked bulk upload; a step inside the session protocol, not a command.
  - op: { method: post, path: "/ddms/v3/welllogs/{record_id}/data" }
    reason: Replaces a record's entire bulk payload in one call, JSON or Parquet. Excluded with the rest of bulk write.
  - op: { method: get, path: "/ddms/v3/welllogs/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/welllogs/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: get, path: "/ddms/v3/welllogs/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: patch, path: "/ddms/v3/welllogs/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/welllogs/{record_id}/sessions/{session_id}/data" }
    reason: Chunked bulk upload; a step inside the session protocol, not a command.
  - op: { method: post, path: "/ddms/v3/welllogs/{record_id}/versions/{version}/data/statistics" }
    reason: Writes computed statistics back; paired with the bulk write path.
  - op: { method: post, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/data" }
    reason: Replaces a record's entire bulk payload in one call, JSON or Parquet. Excluded with the rest of bulk write.
  - op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/sessions" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: get, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: patch, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/sessions/{session_id}" }
    reason: Bulk upload session lifecycle — open, inspect, commit or abandon. A stateful workflow, not a single call.
  - op: { method: post, path: "/ddms/v3/wellpressuretestrawmeasurement/{record_id}/sessions/{session_id}/data" }
    reason: Chunked bulk upload; a step inside the session protocol, not a command.
